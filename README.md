## Main branch contains computing code, and Master branch contains code and data.

Just run draw_scripts.py to automatically compute scores.

If you want to draw figures based on China shapefile, you should prepare it by yourself.
